import Link from 'next/link';import Image from 'next/image';import {ArrowRight,Hotel,MapPinned,Passport,Plane} from '@/lib/icons';
const icons={Passport,Plane,Hotel,MapPinned};
export default function ServiceCard({service}:{service:{id:string,title:string,desc:string,image:string,icon:string}}){const Icon=icons[service.icon as keyof typeof icons]||MapPinned;return <Link href={`/services/${service.id}`} className="group overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-2xl"><div className="relative h-56 overflow-hidden"><Image src={service.image} alt={service.title} fill className="object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-[#071C2C]/70 to-transparent"/><div className="absolute bottom-4 left-4 grid h-11 w-11 place-items-center rounded-2xl bg-white text-[#071C2C]"><Icon size={20}/></div></div><div className="p-6"><span className="inline-flex rounded-full bg-gold/15 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-amber-700">À confirmer par l’agence</span><h3 className="mt-3 text-xl font-extrabold">{service.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{service.desc}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#071C2C]">En savoir plus <ArrowRight size={16}/></span></div></Link>}
