'use client';
import {useState} from 'react';
import {Send,CheckCircle2} from 'lucide-react';
export default function LeadForm({defaultType='TRAVEL_REQUEST',title='Parlez-nous de votre projet'}:{defaultType?:string;title?:string}){const [sent,setSent]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget).entries());data.requestType=defaultType;try{const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();setSent(true);e.currentTarget.reset()}catch{setError('Impossible d’envoyer la demande pour le moment. Appelez-nous directement.')}finally{setLoading(false)}}if(sent)return <div className="success"><CheckCircle2 size={42}/><h2 className="mt-4 text-2xl font-black">Demande envoyée</h2><p className="mt-2 text-slate-600">Merci. L’agence pourra revenir vers vous avec les informations à confirmer.</p></div>;return <form onSubmit={submit} className="form-card"><div><p className="eyebrow-blue">DEMANDE QUALIFIÉE</p><h2 className="mt-2 text-2xl font-black">{title}</h2></div><div className="grid gap-4 md:grid-cols-2"><label className="field"><span>Nom *</span><input required name="name"/></label><label className="field"><span>Téléphone *</span><input required name="phone" type="tel"/></label><label className="field"><span>Email</span><input name="email" type="email"/></label><label className="field"><span>Destination</span><input name="destination" placeholder="Ex. Dubaï"/></label><label className="field"><span>Date souhaitée</span><input name="date" type="date"/></label><label className="field"><span>Voyageurs</span><input name="travelers" type="number" min="1" placeholder="2"/></label><label className="field"><span>Budget</span><input name="budget" placeholder="Sur demande"/></label><label className="field"><span>Type de demande</span><select name="type"><option>Voyage organisé</option><option>Billetterie</option><option>Hôtellerie</option><option>Visa</option><option>Omra</option><option>Autre</option></select></label></div><label className="field"><span>Message</span><textarea name="message" rows={5} placeholder="Décrivez votre projet, vos préférences ou vos questions…"/></label>{error&&<p className="text-sm font-bold text-red-600">{error}</p>}<button disabled={loading} className="btn-blue w-full justify-center">{loading?'Envoi…':'Envoyer ma demande'} <Send size={17}/></button></form>}
