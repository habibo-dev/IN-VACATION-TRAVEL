'use client'
import {useState} from 'react';import {Send,Check} from '@/lib/icons'
export default function LeadForm({defaultType='TRAVEL_REQUEST'}:{defaultType?:string}){const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);const [err,setErr]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setErr('');const data=Object.fromEntries(new FormData(e.currentTarget).entries());data.requestType=defaultType;try{const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error('Erreur');setDone(true)}catch{setErr('Impossible d’envoyer la demande pour le moment. Appelez directement l’agence au 0552 42 65 12.')}finally{setLoading(false)}}if(done)return <div className="rounded-3xl bg-[#071C2C] p-8 text-white"><div className="grid h-12 w-12 place-items-center rounded-full bg-cyan text-[#071C2C]"><Check/></div><h3 className="mt-6 text-2xl font-extrabold">Merci, votre demande a bien été envoyée.</h3><p className="mt-2 text-white/60">L’agence pourra reprendre contact à partir des informations transmises.</p></div>;return <form onSubmit={submit} className="grid gap-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-xl shadow-slate-200/40 sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Type de demande<select name="type" defaultValue="Voyage" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option>Voyage</option><option>Séjour</option><option>Billet</option><option>Hôtel</option><option>Visa</option><option>Autre</option></select></label><label className="text-sm font-semibold">Destination<input name="destination" required placeholder="Ex. Dubai" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold">Date souhaitée<input name="date" type="date" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold">Voyageurs<input name="travelers" type="number" min="1" placeholder="2" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold">Nom complet<input name="name" required placeholder="Votre nom" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold">Téléphone<input name="phone" required placeholder="0552..." className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold sm:col-span-2">Email <span className="font-normal text-slate-400">(facultatif)</span><input name="email" type="email" placeholder="vous@email.com" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold sm:col-span-2">Budget <span className="font-normal text-slate-400">(facultatif)</span><input name="budget" placeholder="Votre budget indicatif" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label><label className="text-sm font-semibold sm:col-span-2">Message<textarea name="message" rows={4} placeholder="Précisez vos besoins, préférences ou contraintes..." className="mt-2 w-full resize-none rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label></div>{err&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{err}</p>}<button disabled={loading} className="flex items-center justify-center gap-2 rounded-xl bg-[#071C2C] px-5 py-4 font-bold text-white disabled:opacity-50">{loading?'Envoi…':'Envoyer ma demande'}<Send size={17}/></button><p className="text-center text-xs text-slate-400">Vos informations sont utilisées uniquement pour traiter votre demande.</p></form>}
