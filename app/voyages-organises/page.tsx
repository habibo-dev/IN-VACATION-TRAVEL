import Link from 'next/link';import {offers} from '@/lib/data';import {TravelCard} from '@/components/Platform';export const metadata={title:'Voyages organisés'};export default function Page(){const intl=offers.filter(o=>o.destination!=='Alger');return <section className="container py-14"><p className="eyebrow-blue">VOYAGES ORGANISÉS</p><h1 className="mt-3 text-5xl font-black">Local & international.</h1><p className="mt-4 max-w-2xl text-slate-600">Découvrez les possibilités. Les contenus ci-dessous sont DEMO jusqu’à confirmation d’un programme réel.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{intl.map(o=><TravelCard key={o.slug} offer={{...o,type:'Voyage organisé'}}/>)}</div><Link href="/demande" className="btn-yellow mt-10">Demander un programme</Link></section>}
