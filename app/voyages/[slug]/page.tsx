import Image from 'next/image';import Link from 'next/link';import {notFound} from 'next/navigation';import {Check,Clock,MapPinned,Plane,Users} from 'lucide-react';import {offers} from '@/lib/data';import {ShareActions} from '@/components/Platform';
export function generateStaticParams(){return offers.map(o=>({slug:o.slug}))}
export default async function TravelDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const o=offers.find(x=>x.slug===slug);if(!o)notFound();return <div><section className="bg-[#071A3D] py-8"><div className="container"><Link href="/voyages" className="text-sm font-bold text-white/70">← Retour aux voyages</Link><div className="mt-6 grid gap-8 lg:grid-cols-[1.25fr_.75fr] lg:items-end"><div><span className="tag">{o.type}</span><h1 className="mt-4 text-5xl font-black text-white">{o.title}</h1><p className="mt-3 text-lg text-white/70">{o.destination} · {o.country}</p></div><div className="lg:text-right"><p className="text-sm text-white/60">Tarif</p><p className="text-2xl font-black text-[#FFD21F]">{o.price?`${o.price.toLocaleString('fr-DZ')} DA`:'Prix sur demande'}</p></div></div></div></section><div className="container py-10"><div className="grid gap-10 lg:grid-cols-[1fr_380px]"><main><div className="relative h-[430px] overflow-hidden rounded-3xl"><Image src={o.image} alt={o.title} fill className="object-cover" priority/></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="card p-5"><Clock/><b className="mt-3 block">Durée</b><span className="text-sm text-slate-500">{o.duration}</span></div><div className="card p-5"><Plane/><b className="mt-3 block">Départ</b><span className="text-sm text-slate-500">{o.departure}</span></div><div className="card p-5"><Users/><b className="mt-3 block">Dates</b><span className="text-sm text-slate-500">{o.date||'Sur demande'}</span></div></div><section className="mt-10"><h2 className="text-3xl font-black">À propos de ce voyage</h2><p className="mt-4 leading-8 text-slate-600">{o.summary}</p></section><div className="mt-10 grid gap-8 md:grid-cols-2"><div><h3 className="text-xl font-black">Inclus</h3><ul className="mt-4 grid gap-3">{o.included.map(x=><li key={x} className="flex gap-2 text-sm"><Check className="text-[#1F3F93]" size={18}/>{x}</li>)}</ul></div><div><h3 className="text-xl font-black">Non inclus / à confirmer</h3><ul className="mt-4 grid gap-3 text-slate-600">{o.notIncluded.map(x=><li key={x} className="flex gap-2 text-sm">• {x}</li>)}</ul></div></div></main><aside className="h-fit rounded-3xl border border-slate-200 bg-white p-6 shadow-xl lg:sticky lg:top-28"><h2 className="text-xl font-black">Ce voyage vous intéresse ?</h2><p className="mt-2 text-sm leading-6 text-slate-600">Les détails, prix et disponibilités sont à confirmer par l’agence.</p><Link href={`/demande?destination=${encodeURIComponent(o.destination)}&type=${encodeURIComponent(o.type)}`} className="btn-blue mt-6 w-full">Demander ce voyage</Link><ShareActions title={o.title}/><div className="mt-6 flex gap-3 rounded-2xl bg-slate-50 p-4 text-xs text-slate-600"><MapPinned size={18}/><span>Destination localisée sur la carte. Les données marquées DEMO ne constituent pas une disponibilité commerciale.</span></div></aside></div></div></div>}
