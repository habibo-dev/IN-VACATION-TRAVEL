import type {MetadataRoute} from 'next';import {services,destinations} from '@/lib/data';const base='https://in-vacation-travel.vercel.app';export default function sitemap():MetadataRoute.Sitemap{const now=new Date();return[{url:base,lastModified:now},{url:`${base}/services`,lastModified:now},{url:`${base}/destinations`,lastModified:now},{url:`${base}/offres`,lastModified:now},{url:`${base}/demande`,lastModified:now},{url:`${base}/a-propos`,lastModified:now},{url:`${base}/contact`,lastModified:now},{url:`${base}/ar`,lastModified:now},...services.map(s=>({url:`${base}/services/${s.id}`,lastModified:now})),...destinations.map(d=>({url:`${base}/destinations/${d.id}`,lastModified:now}))]}
