import Image from 'next/image';import Link from 'next/link';import {destinations} from '@/lib/data';import {ArrowRight} from '@/lib/icons';export default function Destinations(){return <main className="pt-20"><section className="bg-[#071C2C] py-24 text-white"><div className="container"><span className="text-xs font-bold uppercase tracking-[.2em] text-cyan">Destinations</span><h1 className="mt-4 max-w-3xl text-5xl font-extrabold sm:text-6xl">Des idées de départ, à confirmer selon les offres du moment.</h1><p className="mt-5 max-w-2xl text-white/65">Les destinations présentées ici sont des contenus de proposition et doivent être validées avec l’agence avant publication commerciale définitive.</p></div></section><section className="py-20"><div className="container grid gap-7 md:grid-cols-3">{destinations.map(d=><Link key={d.id} href={`/destinations/${d.id}`} className="group overflow-hidden rounded-3xl border border-slate-200 bg-white"><div className="relative h-72"><Image src={d.image} alt={d.title} fill className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="p-6"><span className="text-xs font-bold uppercase tracking-[.2em] text-cyan">{d.country}</span><h2 className="mt-2 text-2xl font-extrabold">{d.title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{d.desc}</p><span className="mt-5 flex items-center gap-2 text-sm font-bold">Demander un séjour <ArrowRight size={16}/></span></div></Link>)}</div></section></main>}
