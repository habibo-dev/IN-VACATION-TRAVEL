import {NextResponse} from 'next/server';import {z} from 'zod';const schema=z.object({name:z.string().min(2),phone:z.string().min(6),email:z.string().email().optional().or(z.literal('')),requestType:z.string().min(2),type:z.string().optional(),destination:z.string().optional(),date:z.string().optional(),travelers:z.string().optional(),budget:z.string().optional(),message:z.string().optional()});export async function POST(req:Request){try{const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({ok:false,error:'Données invalides'},{status:400});const lead={...parsed.data,createdAt:new Date().toISOString()};console.log('[LEAD]',JSON.stringify(lead));if(process.env.LEADS_WEBHOOK_URL)await fetch(process.env.LEADS_WEBHOOK_URL,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(lead)});return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false,error:'Erreur serveur'},{status:500})}}
