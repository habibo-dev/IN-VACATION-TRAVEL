import {TravelCard} from '@/components/Platform';import {offers} from '@/lib/data';export const metadata={title:'Offres'};export default function Page(){return <section className="container py-14"><div className="max-w-3xl"><p className="eyebrow-blue">OFFRES</p><h1 className="mt-3 text-5xl font-black">Les idées du moment.</h1><p className="mt-4 leading-8 text-slate-600">Une vitrine éditoriale sans fausse urgence. Les contenus DEMO servent à illustrer l’expérience et doivent être remplacés par les offres réellement confirmées.</p></div><div className="mt-10 grid gap-5 md:grid-cols-2">{offers.map(o=><TravelCard key={o.slug} offer={o}/>)}</div></section>}
